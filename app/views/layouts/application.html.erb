<!DOCTYPE html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="utf-8" />
    <title>ثمانية</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://cdn.tailwindcss.com"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-100 text-slate-800">
    <header class="bg-white border-b">
      <div class="max-w-6xl mx-auto p-4 flex items-center justify-between gap-3">
        <h1 class="text-xl font-bold">
            <%= link_to "حلقات ثمانية",
                  (editor_signed_in? ? cms_root_path : discovery_episodes_path),
                  class: "px-3 py-2 rounded-xl bg-white border hover:bg-slate-50" %>
        </h1>
        <% if editor_signed_in? %>
       <div class="flex items-center gap-2 -ml-[130px]">
        <%= link_to "＋ حلقة جديدة", new_cms_episode_path,
          class: "px-3 py-2 rounded-xl bg-indigo-600 text-white hover:bg-indigo-700" %>

        <%= button_to "تسجيل خروج",
          destroy_editor_session_path,
          method: :delete,
          form: { class: "inline-block" },
          class: "px-3 py-2 rounded-xl bg-red-600 text-white hover:bg-red-700" %>
      </div>
        <% else %>
          <%= link_to "تسجيل الدخول للمحرّرين", new_editor_session_path,
              class: "px-3 py-2 rounded-xl bg-white border hover:bg-slate-50" %>
        <% end %>
      </div>
    </header>

    <main class="max-w-7xl mx-auto p-6">
      <%= yield %>
    </main>
  </body>
</html>

